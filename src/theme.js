import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#23839c',
    },
    secondary: {
      main: '#9c7a3c',
    },
  },
  simpleGutter: '0.5em',
});

export default theme;
